
<script>
  var primaryLocations = <%= raw @primaryJson.to_json %>
  var alternateLocations = <%= raw @alternateJson.to_json %>
</script>

<div class="container">
  <div class="row">
    <div class="span10">
      <div id="floatingMap">
        <h3 class="head1"><%= action_name %> Map</h3>
        <div id="map_canvas" style='width: 100%; height: 500px;'></div>
          <%= button_tag "Show Alternates", { :class => "btn", :id => "showMarkers" } %>
          <%= button_tag "Hide Alternates", { :class => "btn", :id => "clearMarkers" } %>
      </div>
    </div>
    <div class="span2 sideBar">
      <%= render 'legend' %>
          <%= form_tag({}, {:method => :get}) do %>
            <%= text_field_tag :search, params[:search], :autocomplete => :off %>
            <%= submit_tag "Site Name Search", :name => nil   %>
          <% end %>
    </div>
  </div>
  <br></br>
  <div class='row infoTable'>
    <div class="span12">
      <%= render 'siteTable' %>
    </div>
  </div>
</div>

